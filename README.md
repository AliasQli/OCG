# VRCOCG

A Yu-Gi-Oh OCG prefab for VRChat.

> [!Note]
> **How to Use**<br/>
> 1. Put `DeckInputer` and `Table` inside `VRCOCG` prefab.
> 2. **Manually set distinct `uid` for every `Table` prefab.**
> 3. The advised scale of the prefab(s) is 2.

These are illustrated in the `ExampleScene`.
